import vue from "vue";
import vuex from "vuex";
import axios from 'axios'

vue.use(vuex);

export const store = new vuex.Store({
  state: {},
  mutations: {},
  actions: {}
});

export default store;
